export interface CategoryCountStat {
  categoryId: string;
  categoryName: string;
  recipeCount: number;
}

export interface CategoryRatingStat {
  categoryId: string;
  categoryName: string;
  avgRating: number;
  ratingsCount: number;
}

